# ── LOCAL DEVELOPMENT ──────────────────
# Copy this file to .env and fill in your values

PORT=3000
JWT_SECRET=vcms-pro-secret-key-change-this

# Local PostgreSQL (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vcms_pro
DB_USER=postgres
DB_PASS=Preyansh@732007

# ── VERCEL / PRODUCTION ─────────────────
# Set DATABASE_URL in Vercel dashboard (from Neon)
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
